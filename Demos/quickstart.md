# Quickstart

1. Build core:
   - `swift build --product Core`
2. Run core demo:
   - `swift run Core`
3. Build dashboard dependencies:
   - `cd Dashboard && npm install`
4. Run dashboard:
   - `npm run dev`
